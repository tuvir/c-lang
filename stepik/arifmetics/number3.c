#include <stdio.h>

int main (void){
    int num = 123, sum = 0, a, b, c;
    // while (num != 0)
    // {
        // sum += num % 10;
        // num /= 10;
    // }
    // printf("%d\n", sum);
    // a = num / 100;
    // sum += a;
    // b = num % 100 / 10;
    // sum += b;
    // c = (num % 100) % 10;
    // sum += c;
    // printf("Sum = %d", sum);
    int f = 5;
    printf("%d", f % 10);
    return 0;
}